import { createTheme } from "@material-ui/core";

const theme = createTheme({
	palette: {
		primary: {
			main: "#408cea"
		},
		secondary: {
			main: "#888888"
		}
	},
	typography: {
		fontFamily: "Roboto"
	}
});

export { theme };
